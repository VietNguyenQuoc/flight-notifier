const isEmpty = require("./isEmpty");
const searchDataTemplate = require("./searchDataTemplate");
const formatPrice = require("./formatPrice");

module.exports = {
  isEmpty,
  searchDataTemplate,
  formatPrice
};
